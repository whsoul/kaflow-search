[package]
name = "kaflow-tokenizer"
version = "0.1.0"
edition = "2021"
description = "Kaflow search term tokenizer — interface plus a lightweight default implementation, no dependencies"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
